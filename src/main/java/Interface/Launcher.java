package Interface;

import javafx.application.Application;

public class Launcher {
    public static void main(String[] args) {
        // Redireciona a inicialização para a sua classe que estende Application
        // Substitua 'Main.class' pelo nome correto da sua classe principal do JavaFX
        Application.launch(Main.class, args);
    }
}